import React from "react";
import { Button } from "react-bootstrap";

const Shop = () => {
  return (
    <div className="mt-3">
      <Button variant="primary" size="lg">
        -
      </Button>
      Update Balance
      <Button variant="primary" size="lg">
        +
      </Button>
    </div>
  );
};
export default Shop;